# Active Project Config
# Token target: <100 tokens
# Project-level config only — does NOT change during normal workflow execution
# Loaded once on agent init, not on every step
# Generated by Frappe-Builder | Version: 3.0

project: ""           # Project display name
app: ""               # Frappe app name (e.g., "my_custom_app")
site: ""              # Frappe site name (e.g., "mysite.localhost")
bench_path: ""        # Absolute path to Frappe bench

# Derived at runtime (not stored — computed from above):
# app_path  →  {{bench_path}}/apps/{{app}}
# docs_path →  {{app_path}}/docs
