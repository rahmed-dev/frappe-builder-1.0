# Create Guide Workflow

Purpose: Produce concise user documentation (guides/quick refs) from feature specs with ERPNext UI terminology.

Inputs: Feature/TSD/implementation notes; project/app/site context.

Outputs: User guides or quick references using provided templates.

Entrypoint: `{project-root}/.bmad/frappe-builder/workflows/create-guide/workflow.yaml`

Usage: Invoke via agent command `*guide` (Doc-Writer) or direct workflow path.
