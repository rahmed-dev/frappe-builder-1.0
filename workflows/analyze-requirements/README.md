# Analyze Requirements Workflow

Purpose: Convert messy requirements/notes into a structured BRD with ERPNext module mapping (Standard/Configure/Custom split).

Inputs: Meeting notes or requirement doc; optional state context (project/app/site).

Outputs: Business Requirements Document (BRD) aligned to ERPNext modules.

Entrypoint: `{project-root}/.bmad/frappe-builder/workflows/analyze-requirements/workflow.yaml`

Usage: Invoke via agent command `*analyze` (ERPNext BA) or direct workflow path.
