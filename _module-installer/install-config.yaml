# Frappe-Builder Module Installer Configuration
# Generated by BMAD Builder
# Version: 1.0.0
# Date: 2025-11-20

code: frappe-builder
name: "Frappe-Builder"
default_selected: false

# Welcome message shown during installation
prompt:
  - "Thank you for choosing Frappe-Builder!"
  - "Comprehensive Frappe/ERPNext development ecosystem with 8 specialized agents"
  - "Covers complete SDLC: Requirements → Architecture → Planning → Development → Testing → Documentation"

# Core config values are automatically inherited from BMAD Core:
## user_name
## communication_language
## document_output_language
## output_folder

# ============================================================================
# MODULE-SPECIFIC CONFIGURATION
# ============================================================================

# Frappe Bench Environment (INTERACTIVE)
frappe_bench_path:
  prompt: "Path to your Frappe bench directory"
  default: "{project-root}"
  result: "{value}"

default_site:
  prompt: "Default Frappe site name for development"
  default: "site1.local"
  result: "{value}"

# Module Infrastructure Paths (STATIC)
kb_location:
  result: "{project-root}/.bmad/frappe-builder/knowledge-base"

module_data_path:
  result: "{project-root}/.bmad/frappe-builder/data"

standards_path:
  result: "{project-root}/.bmad/frappe-builder/standards"

templates_path:
  result: "{project-root}/.bmad/frappe-builder/templates"

# Agent Bench Awareness (used by all agents at startup)
apps_directory:
  result: "{frappe_bench_path}/apps"

sites_directory:
  result: "{frappe_bench_path}/sites"

# Module Version (STATIC)
module_version:
  result: "1.0.0"
