# ERPNext-BA Session Memories

Use this file to record brief notes that help future BRD and module-mapping decisions.

---

## Session Context

- User: `{user_name}`
- Current app: `{{current_app}}`
- BRD path: `{{brd_path}}`

---

## Requirements Analysis

- ERPNext modules mapped: `[requirement] → [ERPNext module] (because [fit])`
- Custom vs Standard calls: `[feature] → Standard / Configure / Custom (reason)`

---

## Session Variables

- `{{current_app}}` = [app name]
- `{{app_path}}` = `{project-root}/apps/{{current_app}}`
- `{{brd_path}}` = `{{app_path}}/docs/brd`

---

Last updated: [date]
